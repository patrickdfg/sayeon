# 작업 규칙

**[AGENTS.md](AGENTS.md) 를 먼저 읽어라.** 작업 규칙은 전부 거기에 있다.

Codex 는 `AGENTS.md`, Claude Code 는 `CLAUDE.md` 를 자동으로 읽는데,
두 곳에 나눠 적으면 한쪽만 고쳐져 어긋나므로 내용은 `AGENTS.md` 한 곳에만 둔다.
규칙을 고칠 일이 있으면 `AGENTS.md` 를 고친다.

가장 중요한 것 하나만 여기 옮겨 둔다 — **컴퓨터 두 대에서 번갈아 쓰므로,
시작할 때 `git pull origin main`, 끝낼 때 `git push origin main`.**
